{
  plugins.blink-cmp = {
    enable = true;

    settings = {
      sources.default = ["lsp"];

      # TODO: Keymaps
      #keymap = {
      #  preset = "none";

      #  "<Up>" = ["select_prev" "fallback"];
      #  "<Down>" = ["select_next" "fallback"];
      #  "<C-p>" = ["select_prev" "fallback"];
      #  "<C-n>" = ["select_next" "fallback"];
      #};
    };
  };
}
